<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="12dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="12dp"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="@dimen/card_corner"
    app:cardElevation="@dimen/card_elevation"
    app:cardUseCompatPadding="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/spacing_md">

        <ImageView
            android:id="@+id/product_image"
            android:layout_width="100dp"
            android:layout_height="@dimen/product_image_height"
            android:scaleType="centerCrop"
            android:contentDescription="@string/default_product_name"
            android:src="@drawable/ic_launcher_background"
            android:layout_alignParentStart="true"
            android:layout_marginEnd="@dimen/spacing_md" />

        <TextView
            android:id="@+id/product_name"
            style="@style/TextAppearance.Duan1.Title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_toEndOf="@id/product_image"
            android:ellipsize="end"
            android:maxLines="2" />

        <TextView
            android:id="@+id/product_category"
            style="@style/TextAppearance.Duan1.Subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/product_name"
            android:layout_marginTop="4dp"
            android:layout_toEndOf="@id/product_image" />

        <!-- Price: place directly under category and use price color for visibility -->
        <TextView
            android:id="@+id/product_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/product_category"
            android:layout_marginTop="6dp"
            android:layout_toEndOf="@id/product_image"
            android:textColor="@color/priceColor"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Reviews/ratings removed as per request -->

        <!-- Add to cart button - kept at the end -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_to_cart"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:backgroundTint="@color/md_theme_primary"
            android:insetLeft="8dp"
            android:insetRight="8dp"
            android:text="@string/add"
            android:textColor="@color/md_theme_onPrimary"
            app:cornerRadius="8dp" />

        <!-- Wishlist (heart) placed below Add-to-cart as requested -->
        <ImageButton
            android:id="@+id/wishlist_button"
            android:layout_width="@dimen/icon_size"
            android:layout_height="@dimen/icon_size"
            android:layout_below="@id/btn_add_to_cart"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_circle_heart"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_favorite_border"
            android:contentDescription="@string/favorite" />

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>
